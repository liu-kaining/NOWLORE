# ADR-0003：真实发行必须人工审批

日期：2026-09-01  
状态：Accepted

## 背景

热点系统容易受到误报、提示注入、版权争议和突发事件影响；Solana 主网交易不可逆，AI 自动发行的错误成本高。

## 决策

发现、评估和草稿生成可以自动化；真实发行必须有与内容哈希绑定的人工审批、资产校验、成功模拟、主网开关和幂等键。AI 无权调用签名器。默认 `dry-run` 与 signer disabled。

## 结果

牺牲数十秒至数分钟的完全自动速度，换取可审计的责任链和防误发能力。后续即使使用多签或策略审批，也不能移除内容哈希与模拟闸门。
